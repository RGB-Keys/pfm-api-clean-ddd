volumes:
  postgres_pfm_local:

services:
  postgres:
    image: postgres
    container_name: postgres_pfm_local
    environment:
      POSTGRES_PASSWORD: 'dev'
      POSTGRES_DB: 'pfm'
    ports:
      - 9004:5432
    volumes:
      - postgres_pfm_local:/var/lib/postgres